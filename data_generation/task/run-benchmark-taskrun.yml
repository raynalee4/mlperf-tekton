apiVersion: tekton.dev/v1alpha1
kind: TaskRun
metadata:
  name: run-run
  namespace: mlperf
spec:
  taskRef:
    name: run-benchmark
  serviceAccount: data-generation
  inputs:
    resources:
      - name: build-image
        resourceRef:
          name: build-image